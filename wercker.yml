box: debian
build:
  steps:
    - script:
        name: Install make
        code: |
          apt-get update -qq
          apt-get install -y make
    - script:
        name: Build
        code: |
          make ci-build
