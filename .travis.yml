language: c
sudo: enabled
matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required
    - os: linux
      dist: precise
      sudo: required
before_install:
  - sudo make build_before
script:
  - make pkg-build
  - ls -l dist
  - make pkg-test
sudo: required
