version: '1.0'


steps:
  build1:
    image: mwendler/cowsay
    description: build1
    commands:
        - apt-get update -qq
        - apt-get install -y make
        - make build_before
        - make pkg-build
        - make pkg-test
